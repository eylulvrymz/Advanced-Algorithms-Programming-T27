# LAB 10
Elenie Girma Wakjira: Exercise 1

Eylül Safiye Varyemez: Exercise 2

shen yan：exercise3
**Exercise 1:** For this exercise we implemented three functions to find the largest group of users that can be invited to an event without any two conflicting guests attending together. is_valid_invitation(invited, graph) checks whether a given list of users forms a valid independent set by iterating over each invited user's conflict list and verifying that none of their neighbors are also invited, returning True only if no conflicting pair exists. find_max_invitations_exact(graph) finds the true maximum independent set using backtracking with pruning: it explores subsets of users recursively, and at each step checks whether the current partial solution plus all remaining candidates could possibly beat the best solution found so far, if not, it abandons that branch immediately, which significantly cuts down the search space compared to brute force. find_max_invitations_greedy(graph) is a faster heuristic that repeatedly picks the user with the fewest conflicts among those still available, adds them to the invited set, then removes them and all their neighbors from consideration, repeating until no users remain. We tested all three against empty graphs, single nodes, fully connected graphs, no-edge graphs, path graphs, even cycles, star graphs, and a branch graph, and also compared greedy and exact results side by side on small instances.

**Complexity Analysis:** is_valid_invitation runs in O(k²) where k is the number of invited users, or O(E) in the worst case, since for each invited user it scans their neighbor list and checks membership in the invited set. find_max_invitations_exact runs in O(2^N · E) in the worst case since it may explore all 2^N subsets and validate each one, but pruning makes it significantly faster in practice and feasible for N ≤ 40 as required. find_max_invitations_greedy runs in O(N²) in a straightforward implementation since at each of up to N rounds it scans remaining nodes and their neighbors to find the minimum-degree candidate, making it practical even for very large graphs in the millions of nodes.

**Exercise 2:** We started by tackling the exact solution through dynamic programming — we built a 2D table where each cell tracks the best reach achievable with a given number of users and remaining budget, then traced back through it to figure out exactly which users were picked. This directly answers the professor's request for an O(N × budget) solution that works for N up to 500 and budget up to 10,000. We also wrote the small `is_within_budget` helper which just adds up the costs of a chosen group and checks if they fit, simple but necessary. For the greedy approach, we ranked users by how much reach you get per dollar spent, then kept picking the best value users until the budget ran out. Finally, we ran both methods on the same inputs to compare them, and the results clearly showed that greedy doesn't always win — in one of our tests it got a reach of 12 while the exact method found 13, and in our deliberate counterexample it only reached 7 versus the optimal 10, which is exactly the kind of comparison the professor was looking for.

**Complexity Analysis:** The DP approach works well because instead of blindly testing every possible combination of users, it breaks the problem into smaller pieces and saves the results along the way, which is what keeps it at O(N × budget). That said it has a clear breaking point — once you start talking about millions of users and billion dollar budgets the table becomes too large to even store in memory, and if costs are real numbers rather than clean integers the whole approach falls apart completely. That is where greedy comes in, it just ranks users by how much reach you get per dollar and keeps picking until the money runs out, which makes it fast and scalable no matter how large the input gets. The catch is that it is not always right, we saw this directly in our counterexample where greedy got stuck at a reach of 7 while the exact method found 10 by thinking a step ahead. In the real world though, platforms with hundreds of millions of users do not have the luxury of running exact methods, so they take the greedy answer knowing it is close enough and move on.

exercise3:This lab implements a balanced graph partitioning algorithm. The goal is to split users into two groups while minimizing the number of friendships between groups, ensuring each group contains at least 40% of all users. The countCrossEdges method counts edges between two groups in O(E). The findBalancedPartitionGreedy method uses a greedy approach with random initialization, repeatedly moving single nodes to reduce cross edges. The findBalancedPartitionLocalSearch method runs the greedy algorithm multiple times with different random starts and returns the best result found. Unit tests are provided in exercise3test.java using JUnit 5.